# ─── Firebase Client (variáveis públicas) ────────────────────────────────────
# Encontre no Console Firebase → Configurações do projeto → Seus apps → SDK
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# ─── Firebase Admin SDK (variáveis privadas) ──────────────────────────────────
# Encontre no Console Firebase → Configurações do projeto → Contas de serviço → Gerar nova chave privada
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# A chave privada deve ter as quebras de linha substituídas por \n
FIREBASE_PRIVATE_KEY=

# ─── Google Cloud / Vertex AI ─────────────────────────────────────────────────
# Em produção (Cloud Run) as credenciais são automáticas (ADC).
# Em desenvolvimento local, aponte para o arquivo JSON da service account:
# GOOGLE_APPLICATION_CREDENTIALS=/caminho/para/service-account.json
#
# O projeto GCP deve ter as APIs habilitadas:
#   - Vertex AI API
#   - Cloud Firestore API
GOOGLE_CLOUD_PROJECT=
